import { lusitana } from '@/fonts/fonts';

export default function IntelliceCVLogo() {
  return (
    <h2
      className={`${lusitana.className} text-2xl leading-none text-black antialiased dark:text-white`}
    >
      Intellice CV
    </h2>
  );
}
